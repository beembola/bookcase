import React from "react";
const  AddButton = props => {
    return (
        <button className="btn btn-primary" onClick={props.AddButton}>
        Add
        </button>
    );

};
export default AddButton;